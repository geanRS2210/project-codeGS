import styled, { css, StyledComponent } from 'styled-components';
import { Theme } from '../../../styles/theme';

export const StyledMajorAds: StyledComponent<
  'div',
  any,
  object,
  never
> = styled.div`
  ${({ theme }: Theme) => css``}
`;
